# Skill Registry — nitido

Generated by sdd-init for orchestrated SDD work in this project.

## Project conventions

- Global instructions: `~/.claude/CLAUDE.md` (orchestrator delegation rules, SDD workflow).
- No project-level `AGENTS.md`, `CLAUDE.md`, `.cursorrules`, `GEMINI.md`, or `copilot-instructions.md`.

## Relevant skills (Flutter/Dart context)

| Skill | Path | Trigger | Use for |
|---|---|---|---|
| `context7` | `~/.claude/skills/context7/SKILL.md` | "use context7", asking about a library/API/framework | Fetch up-to-date docs before writing code that depends on a Dart/Flutter package |
| `frontend-design` | `~/.claude/skills/frontend-design/SKILL.md` | building UI/components/pages | Design polished widget UIs for dashboard tiles |
| `simplify` | (built-in) | review changed code | Review widget code for reuse/quality after each task |
| `review` | (built-in) | review a PR | Pre-merge PR review |
| `security-review` | (built-in) | security review of pending changes | Security audit before publishing |
| `update-config` | (built-in) | settings.json, hooks, permissions | Project-level Claude Code settings if needed |

## NOT relevant for Flutter nitido

- `next-best-practices`, `next-upgrade`, `react-best-practices` — React/Next.js specific
- `web-design-guidelines` — Web-specific accessibility/UX guidelines
- `claude-api` — Anthropic SDK specific (not used by nitido)

## Coding skills to load during sdd-apply

When implementing Flutter widgets/services, prefer to load `context7` before writing code that touches an unfamiliar pub.dev package. No Flutter-specific coding skill is registered globally.
